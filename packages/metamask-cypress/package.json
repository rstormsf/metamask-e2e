{
  "name": "metamask-cypress",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "tslint": "tslint -p ./tsconfig.json -e 'node_modules/**/*' -e '**/node_modules/**/*' '**/*.ts'",
    "tslint:fix": "tslint --fix --format stylish -p ./tsconfig.json -e 'node_modules/**/*' -e '**/node_modules/**/*' '**/*.ts'",
    "tsc": "tsc --noEmit",
    "lint": "yarn tslint && yarn tsc",
    "lint:fix": "yarn tslint:fix && yarn tsc",
    "test": "yarn lint",
    "test:fix": "yarn lint:fix"
  },
  "dependencies": {
    "metamask-puppeteer": "0.0.1",
    "node-fetch": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^10.12.0",
    "ts-essentials": "^1.0.0"
  },
  "peerDependencies": {
    "cypress": "^3.1.1"
  }
}
