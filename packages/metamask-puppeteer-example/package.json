{
  "name": "metamask-puppeteer-example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "ts-node -r tsconfig-paths/register ./src/index.ts",
    "format": "prettier --list-different '**/*.{ts,json,md, gql}'",
    "format:fix": "prettier --write '**/*.{ts,json,md,gql}'",
    "tslint": "tslint -p ./tsconfig.json -e 'node_modules/**/*' -e '**/node_modules/**/*' '**/*.ts'",
    "tslint:fix": "tslint --fix --format stylish -p ./tsconfig.json -e 'node_modules/**/*' -e '**/node_modules/**/*' '**/*.ts'",
    "tsc": "tsc --noEmit",
    "lint": "yarn format && yarn tslint && yarn tsc",
    "lint:fix": "yarn format:fix && yarn tslint:fix && yarn tsc",
    "test": "yarn lint && yarn start",
    "test:fix": "yarn lint:fix && yarn start"
  },
  "devDependencies": {
    "@types/node": "^10.12.0",
    "@types/puppeteer": "^1.10.0",
    "prettier": "^1.14.3",
    "ts-essentials": "^1.0.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "tslint-language-service": "^0.9.9",
    "typescript": "^3.1.3",
    "typestrict": "^1.0.1"
  },
  "dependencies": {
    "puppeteer": "^1.10.0",
    "metamask-puppeteer": "0.0.1"
  }
}
